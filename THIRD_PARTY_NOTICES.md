# Third-party components

The application uses the Python standard library and browser-native HTML, CSS and
JavaScript. This source release vendors no third-party packages, fonts or model weights.

Python, Git, Ollama and any model are separate installations. Their respective licenses
and system requirements apply independently of EvidenceDesk's MIT license. The tested
Ollama model name `qwen3.6:35b` identifies a development runtime; it is not a distributed
model or a promise of availability, licensing terms, hardware suitability or quality.
Consult the exact model's upstream terms before obtaining or using it.

The included diffs, code-review examples and evaluation outputs concern invented,
synthetic files. They do not contain a user's repository or customer data. Codex AI agents
assisted in creating project code and documentation; synthetic fixture authorship and
model-result evaluation are disclosed in the QA records.
